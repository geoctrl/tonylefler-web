import { Button } from "stem-ui";

### iconOnly

```
iconOnly?: Icons | IconProps | React.ReactNode;
```

Changes button to be a square icon-only button. Children and other icons are
ignored.

<Button iconOnly="star">Icon left</Button>

```tsx
<Button iconOnly="star">Icon left</Button>
```
